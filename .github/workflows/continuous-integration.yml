name: Continuous Integration

on:
  push:
  pull_request:

jobs:
  fuzzing:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout code
      - name: Check out repository
        uses: actions/checkout@v4

      # Step 2: Set up Python 3.10
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # Step 3: Install dependencies
      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          pip install -r requirements.txt

      # Step 4: Run fuzz tests
      - name: Run fuzzing
        run: |
          echo "Running fuzz.py..."
          python3 fuzz.py

      # Step 5: Display fuzz_forensics.log
      - name: Display Fuzz Logs
        if: always()
        run: |
          echo "==== Fuzz Forensics Log ===="
          cat fuzz_forensics.log

      # Step 6: Upload fuzz_forensics.log artifact
      - name: Upload Fuzz Logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fuzz-logs
          path: fuzz_forensics.log
          if-no-files-found: ignore